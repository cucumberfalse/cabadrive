map $uri $cache_control {
  default            "";
  ~^/assets/         "public, max-age=31536000, immutable";
  ~^/content/assets/ "public, max-age=86400, stale-while-revalidate=604800";
  /sw.js             "no-cache";
}

server {
  listen 8080;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  gzip on;
  gzip_vary on;
  gzip_min_length 1024;
  gzip_comp_level 6;
  gzip_types application/javascript application/json image/svg+xml text/css text/javascript;

  add_header X-Content-Type-Options "nosniff" always;
  add_header X-Frame-Options "DENY" always;
  add_header Referrer-Policy "strict-origin-when-cross-origin" always;
  add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
  add_header Content-Security-Policy "default-src 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self'; connect-src 'self'; base-uri 'none'; frame-ancestors 'none'" always;
  # NS-8: no `always` here on purpose. `always` would apply the long-lived,
  # immutable Cache-Control to error responses too, so an /assets/* miss
  # (try_files ... =404) would be cached as immutable and pin the failure in
  # browsers even after the file returns. Without `always`, nginx adds the
  # header only for 2xx/3xx; security headers keep `always` to cover error pages.
  add_header Cache-Control $cache_control;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location /assets/ {
    try_files $uri =404;
  }

  location /content/assets/ {
    try_files $uri =404;
  }

  location = /sw.js {
    try_files $uri =404;
  }
}
