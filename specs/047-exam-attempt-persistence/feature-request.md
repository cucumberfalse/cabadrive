# Feature Request: Стартовый экран экзамена + персист активной попытки + guard ухода (ТЗ-P1, слайс 2)

## Intake Metadata

- Feature ID: `047-exam-attempt-persistence`
- Роль intake: Analyst
- Назначенный worktree: `/Users/chap/devel/cabadrive-claude/repo/.claude/worktrees/047-exam-attempt-persistence`
- Назначенная ветка: `claude/047-exam-attempt-persistence`
- Verified base от Orchestrator: `origin/main` = `ae5f9804676ff5ebd09244927732d160b9ba37b6` (merge PR #211 «feat(progress): reset confirmation, undo, export/import» — слайс 1 ТЗ-P1).
- Подтверждение базы при intake: локальный `HEAD` worktree = `ae5f9804` = назначенный base; дерево чистое до записи этого артефакта (`git status --short` пуст).
- Дата intake: 2026-07-23
- Parallel-work warning: возможна параллельная работа других агентов в соседних worktree/ветках/PR. Их dirty diffs, ветки, коммиты, PR и процессную память сохранять; не мутировать.
- Scope Analyst: только этот intake-артефакт. Никаких `spec.md`, `plan.md`, `tasks.md`, кода, тестов, коммитов, push, PR, ревью, мержей.

## Исходный запрос и выбор приоритета

Владелец репозитория попросил: «Выбери в docs/improvements самую важную нереализованную доработку в порядке приоритета и реализуй её по гайдлайнам проекта».

По рекомендованной последовательности этапа 1 (`docs/improvements/README.md`) и плану слайсов ТЗ-P1 (`docs/improvements/priority/01-usability.md` §7) уже реализованы: ТЗ-22 (LICENSE/атрибуция), ТЗ-16 (quality tooling), ТЗ-06 (централизованный store прогресса, PR #210) и **слайс 1 ТЗ-P1** (FR-A1/A2/A3 — подтверждение сброса + undo + экспорт/импорт, PR #211). Следующий пункт плана — **слайс 2 (критический): FR-B1, FR-A4, FR-A5** («Порядок = убывание ущерба для пользователя. Каждый пункт — отдельный PR»).

Данный intake оформляет ровно **слайс 2 ТЗ-P1: FR-B1 + FR-A4 + FR-A5** (стартовый экран экзамена + персист активной попытки + guard ухода/закрытия) как один work cycle: одна ветка, один PR. Слайс 3 (FR-B2/B3/B4) и все остальные слайсы/требования ТЗ-P1 (группы В, Г, Д, Е и FR-A6) — вне scope и оформляются отдельными циклами.

## Контекст продукта и проблема

Cabadrive — статический local-first React/Vite тренажёр теории вождения CABA для русскоязычных пользователей; бэкенда нет, весь прогресс живёт в localStorage браузера. Экзаменационная попытка сегодня — единственное состояние, которое молча гибнет: это один из трёх сценариев молчаливой потери данных, на устранение которых нацелено всё ТЗ-P1 (§12 метрики: «Сценарии молчаливой потери данных: 3 → 0»; слайс 1 закрыл `reset`, слайс 2 закрывает `exam-unmount` и `reload`).

Факты по текущему коду (проверены на base `ae5f9804`; номера строк актуальны на этом HEAD и сместились относительно цитат в ТЗ-P1 после мержей PR #208–#211 — исполнитель обязан перепроверять их на своём HEAD):

- **Экзамен стартует мгновенно, стартового экрана нет.** `function ExamView()` (`src/App.tsx:1095`) на маунте сразу выбирает набор вопросов через `selectExamSet(data.questions, data.examFormat.questionCount, data.examFormat.questionOrderRule)` (`src/App.tsx:1096-1104`) и инициализирует `timeRemaining = data.examFormat.timeLimitMinutes * 60` (`src/App.tsx:1108`). Вкладка «Экзамен» рендерит `<ExamView />` напрямую (`src/App.tsx:5434`), кнопка вкладки вызывает `selectView("exam")` (`src/App.tsx:5381`). Случайный клик по вкладке = начатая попытка.
- **Таймер запускается немедленно на маунте.** `useEffect` создаёт `window.setInterval(..., 1000)`, который тикает уже с первого рендера и при `value <= 1` вызывает `finish(answers)` (`src/App.tsx:1135-1148`). До нажатия какой-либо кнопки «Начать» отсчёт уже идёт — прямо противоречит FR-B1.
- **Активная попытка молча гибнет.** Всё состояние ExamView — локальный React `useState`: `position`, `answers`, `finished`, `timeRemaining`, `resultScore` (`src/App.tsx:1105-1109`). Прогресс пишется в store только в `finish()` через `dispatchProgress({ type: "finishExam", answers: finalAnswers, attempt })` (`src/App.tsx:1113-1133`). Переключение вкладки размонтирует `ExamView` (условный рендер `{view === "exam" && <ExamView />}`, `src/App.tsx:5434`) — все ответы и остаток времени теряются; перезагрузка страницы — то же. Нет `beforeunload`, нет guard-диалога, нет персиста активной попытки (grep по `src/App.tsx`: `beforeunload`, `visibilitychange`, `cabadrive.exam-attempt` — 0 совпадений).
- **Слайс 1 (#211) оставил переиспользуемые заготовки, которые слайс 2 обязан переиспользовать (принцип Simplicity):**
  - `src/components/ConfirmDialog.tsx` — единственный компонент в `src/components/`: нативный `<dialog>` (`showModal()` даёт фокус-ловушку, `Esc` через `onCancel`, `::backdrop`, возврат фокуса на opener). Пропсы: `open`, `title`, `children`, `confirmLabel`, `cancelLabel` (default «Отмена»), `onConfirm`, `onCancel`, `confirmDisabled`, `danger`. Именно его ТЗ-P1 §6 предписывает для guard-диалога FR-A5.
  - Паттерн header-notice/статус-панели: тип `ProgressNotice` (`src/App.tsx:5033`), состояние `headerNotice` (`src/App.tsx:5119`), функция-текст `progressNoticeText` (`src/App.tsx:5035`), `dismissHeaderNotice` (`src/App.tsx:5153`) и рендер блока `progress-notice` (`src/App.tsx:5305-5320`). Тот же слот подходит для «Продолжить попытку (осталось MM:SS)» / «Отменить» из FR-A4.
  - `src/progressResetSafety.ts` — образец изолированного storage-хелпера: `StorageLike`-приёмник (`Pick<Storage, "getItem" | "setItem" | "removeItem">`), `try/catch` вокруг каждой операции, session-scoped ключ в неймспейсе `cabadrive.*`, чистые функции (`saveUndoSnapshot`/`readUndoSnapshot`/`clearUndoSnapshot`/`exportFileName`), покрытые unit-тестом `tests/progress-reset-safety.test.mjs`. Это готовый шаблон для нового модуля персиста попытки.
- **Константы формата — из `data.examFormat`, не хардкодить.** `content/config/caba-exam-format.json` → `src/data/content.ts` (`examFormat`, строки 2 и 350). Значения на base: `questionCount = 40`, `timeLimitMinutes = 45`, `passingScore = 85`, `canSkipQuestion = true`, `questionOrderRule = "random_questions_from_available_validated_pool"`, `status = "defined"`. Результат-экран уже перечисляет формат из этих полей (`src/App.tsx:1179-1180`) — стартовый экран FR-B1 обязан делать так же (никаких литералов 40/45/85).
- **Персист попытки — ОТДЕЛЬНЫЙ localStorage-ключ, а не схема прогресс-store.** ТЗ-P1 §6: ключ `cabadrive.exam-attempt.v1`; дедлайн хранить как absolute timestamp (`deadline = startedAt + timeLimitMinutes*60_000`) — восстановление корректно после перезагрузки; тик таймера вычисляет remaining из дедлайна. Прогресс-store (`src/progressStore.ts` / `src/progressStoreCore.ts`, ключ `cabadrive.progress.v1`) хранит только завершённые попытки (`examAttempts`) и его контракт ТЗ-06 не пересматривается — активная попытка в его схему не входит.
- **E2e-базис.** `tests/e2e/app.spec.ts` — 66 `test()` на base, гоняются в двух проектах Playwright (`chromium` = Desktop Chrome, `mobile` = Pixel 7) = 132 запуска (снимок «51 теста / 102 запуска» в ТЗ-P1 §8 AC-9 устарел до слайса 1; исполнитель фиксирует фактический счётчик на своём HEAD). Существующие экзаменационные сценарии уже кликают вкладку «Экзамен» и сразу ожидают вопрос/идущий таймер: `tests/e2e/app.spec.ts:1273` («exam mode hides translation and explanation during active attempt and stores score») и `tests/e2e/app.spec.ts:1300` («exam timeout persists exactly one completed attempt»). Введение стартового экрана FR-B1 меняет этот вход — эти тесты придётся обновить (нажать «Начать» перед ожиданием вопроса), сохранив их проверяемое поведение. Unit-тесты — `node --test` над `tests/*.test.mjs`.
- Гайды качества: `pnpm run quality:fast`, `pnpm run format:check`, `pnpm run preflight` перед push (CLAUDE.md, процессная память quality gates).

## Цель

Экзаменационная попытка перестаёт быть источником молчаливой потери данных: пользователь осознанно начинает попытку с экрана формата (таймер до этого не идёт), а начатая попытка переживает перезагрузку страницы и переключение вкладок и может быть корректно продолжена с правильным остатком времени и сохранёнными ответами; уход с вкладки экзамена или закрытие браузера при активной попытке требует явного подтверждения. Ноль сценариев, где случайный клик, обновление или переключение вкладки безвозвратно уничтожают ход экзамена.

## Scope

В scope (ровно три функциональных требования слайса 2 ТЗ-P1, уточнённые под текущий код):

- **FR-B1 (стартовый экран экзамена).** До начала попытки вкладка «Экзамен» показывает стартовый экран-карточку с форматом, взятым из `data.examFormat`: количество вопросов, лимит времени, проходной балл, правила пропуска (`canSkipQuestion`), пометка `status`. Кнопка «Начать» переводит в активную попытку. **Таймер и отсчёт времени не запускаются, пока пользователь не нажал «Начать»** — набор вопросов и `startedAt` фиксируются в момент старта, не на маунте компонента.
- **FR-A4 (персист активной попытки).** В момент старта и при каждом ответе активная попытка сериализуется в localStorage под ключом `cabadrive.exam-attempt.v1` в форме `{ questionIds, answers, startedAt, deadline }`, где `deadline` — абсолютный timestamp `startedAt + data.examFormat.timeLimitMinutes*60_000`. При возврате на вкладку/после перезагрузки, если валидная непросроченная попытка сохранена, пользователю предлагается «Продолжить попытку (осталось MM:SS)» / «Отменить»; продолжение восстанавливает набор вопросов, ответы, позицию и **остаток времени, вычисленный из абсолютного `deadline`** (а не из «замороженного» remaining). «Отменить» очищает сохранённую попытку. Завершение попытки (`finish`) очищает ключ. (Восстановление таймера из `deadline` — часть FR-A4 и в scope; изменения цвета/aria таймера FR-B3 — НЕ в scope.)
- **FR-A5 (guard ухода и закрытия).** При активной (начатой, незавершённой) попытке: попытка уйти с вкладки «Экзамен» на другую вкладку открывает подтверждающий `ConfirmDialog` (переиспользуя компонент слайса 1); подтверждение уводит с вкладки (сохранённая попытка остаётся для последующего «Продолжить»), отказ оставляет на экзамене. Закрытие/перезагрузка вкладки браузера при активной попытке вешает нативное предупреждение через `beforeunload`. `beforeunload` — дополнительная страховка; основная гарантия сохранности — персист FR-A4 (в мобильных браузерах `beforeunload` ненадёжен — ТЗ-P1 §10).
- Доступность диалога по NFR-3 ТЗ-P1: фокус-ловушка, `Esc`, автофокус на безопасной кнопке (наследуется от `ConfirmDialog`); новые интерактивные элементы (кнопка «Начать», кнопки notice-панели) соответствуют паттернам `focus-visible` и touch-таргетам ≥42 px (NFR-2).
- Тесты на новое поведение (unit для чистой логики персиста/вычисления remaining из deadline + e2e-сценарии AC-2 и slice-специфичные) и прогон существующего e2e-набора в обоих проектах Playwright с обновлением затронутых экзаменационных тестов под новый стартовый вход (NFR-1).
- Синхронизация durable-документации (`docs_project/screens/learning-and-exam-flows.md`), если фактическое поведение экзаменационного экрана/потока меняется в описанных там местах; отметка выполненности слайса 2 в чекбоксе плана `docs/improvements/priority/01-usability.md` — на усмотрение Architect (в предыдущих циклах статус-поля ТЗ не обновлялись — зафиксировать решение).

Вне scope:

- **FR-B2** (очередь скипов, статус `skipped`, изменение `mistakesFromHistory`), **FR-B3** (цвет таймера `--amber`/`--danger`, aria-live, пометка «время истекло»), **FR-B4** (переработка экрана результата: разбор по темам, список неверных, CTA) — это слайс 3. Текущее поведение «Пропустить» (`skipCurrent`, `src/App.tsx:1157-1166`) и экран результата (`src/App.tsx:1168-1188`) в этом слайсе не переделываются, только адаптируются к тому, что попытка теперь стартует по «Начать» и персистится.
- **FR-A6** (персист сессии обучения в sessionStorage) — слайс 5.
- Все требования групп В, Г, Д, Е ТЗ-P1.
- Изменение схемы/логики прогресс-store (`cabadrive.progress.v1`), cap/quota/миграций — контракт ТЗ-06 (PR #210) не пересматривается; завершённые попытки по-прежнему пишутся через `dispatchProgress({ type: "finishExam", ... })`.
- Декомпозиция App.tsx (ТЗ-04), UI-библиотеки, новые зависимости, роутинг вкладок (ТЗ-05 / FR-G*).
- Синхронизация между устройствами, бэкенд, IndexedDB (ТЗ-15).

## Функциональные требования

1. Вкладка «Экзамен» до старта попытки показывает стартовый экран с форматом из `data.examFormat` (число вопросов, минуты, проходной %, правила пропуска, `status`) и кнопкой «Начать»; никакие литералы 40/45/85 не хардкодятся.
2. Ни таймер, ни отсчёт времени, ни фиксация `startedAt` не происходят до нажатия «Начать»; набор экзаменационных вопросов фиксируется в момент старта.
3. При старте и при каждом ответе активная попытка персистится в localStorage под ключом `cabadrive.exam-attempt.v1` в форме `{ questionIds, answers, startedAt, deadline }` с `deadline = startedAt + data.examFormat.timeLimitMinutes*60_000` (абсолютный timestamp).
4. При возврате на вкладку экзамена или после перезагрузки, если сохранена валидная непросроченная попытка, показывается предложение «Продолжить попытку (осталось MM:SS)» / «Отменить»; MM:SS вычисляется из `deadline` относительно текущего времени.
5. «Продолжить» восстанавливает `questionIds`, `answers`, позицию и остаток времени, рассчитанный из абсолютного `deadline`; продолженная попытка ведёт себя как непрерванная (в т. ч. корректно финиширует по таймеру и по последнему ответу).
6. «Отменить» (отказ от продолжения) очищает ключ `cabadrive.exam-attempt.v1`; успешное завершение попытки (`finish`) также очищает ключ — завершённая попытка живёт только как запись `examAttempts` в прогресс-store.
7. При активной попытке уход с вкладки «Экзамен» на другую вкладку открывает `ConfirmDialog`; подтверждение уводит (персист попытки сохраняется), отказ/Esc/клик мимо оставляют на экзамене.
8. При активной попытке закрытие/перезагрузка вкладки браузера инициирует нативное `beforeunload`-предупреждение; при отсутствии активной попытки `beforeunload` не навешивается.
9. Персист попытки изолирован в отдельном модуле с `StorageLike`-приёмником и `try/catch` (по образцу `src/progressResetSafety.ts`); UI не обращается к `localStorage` напрямую по строковому ключу вне этого модуля.
10. Диалог guard доступен с клавиатуры (фокус-ловушка, Esc, автофокус на безопасной кнопке — из `ConfirmDialog`); кнопка «Начать» и кнопки notice соответствуют touch/focus-паттернам приложения.

## Ожидания приёмки

- **AC-2 (из ТЗ-P1, дословно применимо к слайсу):** перезагрузка страницы посреди экзамена → предложение продолжить с корректным остатком времени и сохранёнными ответами (e2e).
- **Стартовый экран (FR-B1, e2e):** открытие вкладки «Экзамен» показывает карточку формата и кнопку «Начать»; до нажатия таймер стоит (значение времени не убывает); после «Начать» появляется первый вопрос и идёт отсчёт.
- **Guard ухода (FR-A5, e2e):** при активной попытке клик по другой вкладке открывает диалог; отказ оставляет на экзамене с сохранённым прогрессом; подтверждение уводит, а возврат на вкладку предлагает «Продолжить».
- **beforeunload (FR-A5):** при активной попытке навешан обработчик `beforeunload` (проверка регистрации/`preventDefault` семантики; полноценный нативный prompt в Playwright не автоматизируется — зафиксировать способ проверки, напр. dispatch события и assert `defaultPrevented`, либо ручной чеклист).
- **Отмена продолжения (e2e):** «Отменить» в предложении продолжить очищает `cabadrive.exam-attempt.v1`; повторный вход даёт свежий стартовый экран, а не восстановление.
- **Негативный сценарий (e2e/unit, обязателен):** см. раздел ниже — недоступный localStorage и битая/просроченная сохранённая попытка.
- Существующие e2e (`tests/e2e/app.spec.ts`, фактический счётчик `test()` на HEAD × 2 проекта Desktop+Pixel 7) зелёные после адаптации затронутых экзаменационных тестов под стартовый вход; unit-набор `node --test` зелёный (AC-9).
- Гейты `pnpm run quality:fast`, `pnpm run format:check`, `pnpm run preflight` зелёные; в PR записаны фактические свидетельства (verification evidence), а не только AI-резюме.
- Активная попытка хранится только под `cabadrive.exam-attempt.v1`; схема прогресс-store `cabadrive.progress.v1` не расширяется; завершённые попытки по-прежнему идут через `dispatchProgress`.

## Негативные сценарии (обязательные для feature memory)

- **localStorage недоступен / private mode / квота (главный негативный сценарий цикла):** персист активной попытки молча деградирует (обёрнут в `try/catch` по образцу `progressResetSafety.ts`), но экзамен полноценно запускается и проходится в памяти без падения — просто без гарантии восстановления после перезагрузки. Приложение не крашится, ошибка не всплывает пользователю как сбой.
- **Битая / несовместимой версии сохранённая попытка:** нечитаемый JSON, отсутствующие/некорректные поля (`questionIds`/`answers`/`startedAt`/`deadline`), неизвестные `questionIds` (не найдены в текущем пуле) → сохранённая попытка отбрасывается и ключ очищается; пользователь видит чистый стартовый экран, а не «восстановление» в сломанное состояние.
- **Просроченная попытка (`deadline <= now`):** при возврате не предлагается к продолжению, а отбрасывается (ключ очищается); никакого продолжения с отрицательным/нулевым остатком.
- **Случайный клик по вкладке «Экзамен»** не начинает попытку и не запускает таймер (следствие FR-B1) — попытка начинается только по «Начать».
- **Уход с вкладки при активной попытке** не теряет прогресс молча: либо пользователь подтверждает уход и попытка остаётся сохранённой для продолжения, либо остаётся на экзамене (следствие FR-A5 + FR-A4).

## Допущения и default-ответы на открытые вопросы

Пользователь недоступен для Q&A; зафиксированы явные допущения. Architect может уточнить их в `spec.md` без изменения пользовательских гарантий; конфликт с гарантиями — блокер через Orchestrator.

- **A1. Модуль персиста попытки.** Default: новый изолированный модуль (например `src/examAttemptPersistence.ts`) с `StorageLike`-приёмником, `try/catch`, чистыми функциями `save/read/clear` и валидатором/парсером сохранённой попытки, покрытый unit-тестом по образцу `src/progressResetSafety.ts` + `tests/progress-reset-safety.test.mjs`. Точное имя/сигнатуры — за Architect.
- **A2. Ключ и форма записи.** Default: ключ `cabadrive.exam-attempt.v1` (как в ТЗ-P1 §6), значение — сериализованный `{ version, questionIds, answers, startedAt, deadline }` с явной `version` для будущих миграций (в духе версионирования прогресс-store); `deadline` абсолютный. Точная форма/наличие `version`-поля — за Architect, но `deadline` обязателен абсолютным.
- **A3. Момент фиксации набора вопросов.** Default: `selectExamSet(...)` вызывается при нажатии «Начать» (а не в `useMemo` на маунте), чтобы стартовый экран не «сжигал» набор и таймер; при продолжении набор берётся из сохранённых `questionIds` (маппинг id→вопрос по текущему пулу). Реализация вычисления remaining из `deadline` в тике таймера заодно устраняет хрупкий «finish в апдейтере setState» (ТЗ-P1 §6, ТЗ-11) — но выносить прочие правки таймера (цвет/aria) в этот слайс нельзя (FR-B3 = слайс 3).
- **A4. UI предложения «Продолжить/Отменить».** Default: переиспользовать header-notice/статус-панель слайса 1 (тип `ProgressNotice`, слот `progress-notice`) с двумя действиями, либо — на выбор Architect — отдельный ConfirmDialog на входе в экзамен; но guard ухода FR-A5 использует именно `ConfirmDialog`. Единообразие с паттернами слайса 1 предпочтительно.
- **A5. Guard ухода — область перехвата.** Default: перехват в `selectView`/обработчиках кнопок вкладок при `view === "exam"` и активной попытке; подтверждение через `ConfirmDialog`. Учесть также уход в under-states (`pandemia`/manual) через `selectIntroductionEntry`/`selectManualSection` (`src/App.tsx:5232-5244`). Полнота охвата точек ухода — за Architect.
- **A6. beforeunload.** Default: `window.addEventListener("beforeunload", ...)` навешивается только при активной попытке и снимается при её завершении/отмене; `event.preventDefault()` + `returnValue` по актуальной семантике. Признаётся ограничение мобильных браузеров (ТЗ-P1 §10) — основная защита остаётся за FR-A4.
- **A7. Обновление существующих экзаменационных e2e.** Default: тесты `tests/e2e/app.spec.ts:1273` и `:1300` адаптируются добавлением шага нажатия «Начать» перед ожиданием вопроса/таймаута, без ослабления их проверок (счёт, скрытие перевода, ровно одна завершённая попытка). Точечные правки — за Implementation Agent под dispositions Architect.
- **A8. Компонент диалога.** Default: переиспользовать существующий `src/components/ConfirmDialog.tsx` (не создавать новый и не обобщать сверх нужд слайса). Стартовый экран — не диалог, а обычная секция во `workspace` в стиле результат-экрана.
- **A9. Номера строк.** Все ссылки на строки в этом документе проверены на base `ae5f9804`; исполнитель обязан перепроверять их на своём HEAD.

## Риски и меры

| Риск | Вероятн. | Влияние | Мера |
|---|---|---|---|
| Стартовый экран ломает существующие экзаменационные e2e (`:1273`, `:1300`), кликающие вкладку и сразу ждущие вопрос | Высокая | Красный CI | Допущение A7: добавить шаг «Начать» в затронутые тесты, сохранив их проверки; обязательный прогон обоих проектов до PR |
| Восстановление остатка времени из «замороженного» remaining вместо `deadline` даёт неверный отсчёт после перезагрузки | Средняя | AC-2 не выполнен | `deadline` абсолютный (A2); тик вычисляет remaining из `deadline - now`; unit-тест на вычисление remaining + e2e reload-round-trip |
| localStorage недоступен (private mode/квота) — падение при записи попытки | Средняя | Краш экзамена | `try/catch`-обёртка в изолированном модуле (A1), негативный тест на недоступный storage; экзамен идёт в памяти |
| Битая/просроченная/чужая сохранённая попытка восстанавливается в сломанное состояние | Средняя | Порча UX, «застрявший» экзамен | Строгая валидация/парсинг с отбрасыванием и очисткой ключа (негативный сценарий), покрыто unit/e2e |
| `beforeunload` ненадёжен в мобильных браузерах | Высокая | Частичная защита | Основная гарантия — персист FR-A4; `beforeunload` заявлен как дополнение (ТЗ-P1 §10) |
| Guard ухода не покрывает все точки навигации (under-states, кнопки вкладок) | Средняя | Утечка ухода без подтверждения | Централизовать проверку активной попытки в `selectView`/навигационных обработчиках (A5); e2e на уход по разным вкладкам |
| Конфликт с параллельной декомпозицией App.tsx (ТЗ-04) и другими P1-циклами | Средняя | Merge-конфликты | Площадь в App.tsx минимальна (ExamView + guard в selectView + notice-слот); порядок «слайсы 1–4 до декомпозиции» зафиксирован в ТЗ-P1 §10 |

## Источники

- `docs/improvements/priority/01-usability.md` — FR-B1/FR-A4/FR-A5 (§5), дизайн стартового экрана/персиста/дедлайна и ConfirmDialog (§6), план слайсов (§7, слайс 2 критический), AC-2 и AC-9 (§8), план тестирования (§9), риски (§10), затрагиваемые файлы (§11), метрики 3→0 (§12), NFR-1..3.
- `docs/improvements/README.md` — приоритетная последовательность этапа 1.
- `specs/046-progress-reset-safety/feature-request.md` — формат intake слайса 1, паттерны ConfirmDialog/header-notice/изолированного storage-хелпера, граница «FR-A4/A5 — слайс 2» (закрывается настоящим циклом).
- `src/App.tsx` — `ExamView` (1095-1221): маунт-старт (1096-1108), таймер (1135-1148), `finish` (1113-1133), `record`/`skipCurrent` (1150-1166), результат-экран (1168-1188); `selectView` (5211-5230), навигационные обработчики (5232-5244), кнопка вкладки «Экзамен» (5381), рендер `<ExamView />` (5434); header-notice паттерн (5033-5320).
- `src/components/ConfirmDialog.tsx` — переиспользуемый диалог для FR-A5.
- `src/progressResetSafety.ts`, `tests/progress-reset-safety.test.mjs` — образец изолированного storage-модуля с `StorageLike` и unit-тестом.
- `src/progressStore.ts`, `src/progressStoreCore.ts` — публичный API прогресс-store (ключ `cabadrive.progress.v1`, `StorageLike`, `finishExam`); контракт ТЗ-06 не пересматривается.
- `content/config/caba-exam-format.json`, `src/data/content.ts` — источник констант формата (`examFormat`), не хардкодить.
- `tests/e2e/app.spec.ts` (экзаменационные сценарии :1273, :1300), `playwright.config.*` (проекты `chromium`+`mobile`) — тестовый базис и затронутые тесты.
- `docs_project/screens/learning-and-exam-flows.md` — контекст экранов экзамена; синхронизировать при изменении описанного потока.
- `.specify/memory/constitution.md`, `AGENTS.md` (раздел Analyst), `CLAUDE.md` — процессные границы, гейты качества.
- Внешние исследования не требовались: требования полностью определены в авторизованной владельцем durable-спецификации улучшений.

## Границы ролей и handoff

- Analyst создал только этот intake-артефакт и передаёт управление Orchestrator.
- Architect создаёт `spec.md`, `plan.md`, `tasks.md`: финализирует модуль/ключ/форму персиста (A1/A2), момент фиксации набора и вычисление remaining из `deadline` (A3), UI предложения продолжить и область guard-перехвата (A4/A5), политику `beforeunload` (A6), план адаптации существующих e2e (A7), матрицу верификации и cycle PR set.
- Implementation Agent работает только по полной feature memory в назначенном worktree/ветке/PR-слайсе; фиксирует фактические свидетельства и feedback для диспозиции Architect.
- Review Agent проверяет диф и соответствие feature memory без правок кода.
- Orchestrator ведёт PR, проверки, финальные валидации Architect→Analyst, guard текущего head и merge; cleanup — только назначенный Cleanup Agent.

## Initial Cycle Context

На момент intake PR по этому фичеру не существует. Handoff-контекст Analyst: ветка `claude/047-exam-attempt-persistence` в `/Users/chap/devel/cabadrive-claude/repo/.claude/worktrees/047-exam-attempt-persistence` от verified `origin/main` `ae5f9804676ff5ebd09244927732d160b9ba37b6` (= PR #211, слайс 1 уже смержен). Orchestrator может явно продолжить этот latest-main контекст как единственный implementation PR slice либо назначить отдельный свежий worktree; любой дополнительный слайс фиксируется в feature memory.

## Final Analyst Validation Notes

Append-only секция Analyst; заполняется только по явному запросу Orchestrator после прохождения финальной валидации Architect.
