# ТЗ-19: Управление весом репозитория (316 МБ контента, 433 МБ .git)

| Поле | Значение |
|---|---|
| Приоритет | P2 |
| Категория | Инфраструктура репозитория |
| Оценка трудоёмкости | L |
| Статус | Предложено |
| Дата | 2026-07-11 |

## 1. Контекст и проблема

Весь контент закоммичен в обычный git без LFS: content/ = 316 МБ (2915 файлов), .git = 433 МБ. Раскладка: **validation/ 105 МБ** (QA-скриншоты и кропы — доказательная база аудитов), **official-documents/ 77 МБ** (из них 68 МБ — PDF мануала GCBA), **assets/ 71 МБ** (раздаваемые страницы/кропы/картинки), **primary-sources/ 31 МБ** (JSON). Каждый новый визуальный evidence-кроп или пересжатие наращивает историю необратимо; клон уже занимает минуты (наблюдалось при подготовке этого аудита: >2 мин на быстрой сети). Дополнительно: assets дублируются трижды на диске (content/assets → public/content/assets → dist). Осознанная причина текущего устройства — воспроизводимые контент-гейты (sha256-пины, byte-exact evidence) — должна быть сохранена.

Планируемая модернизация изображений ([ТЗ-P3](./priority/03-image-quality.md)) добавит мастера и производные — без стратегии рост ускорится.

## 2. Цели

Остановить необратимый рост истории; клон для разработки ≤ 1 мин; контент-гейты продолжают работать byte-exact.

## 3. Не-цели

Немедленная перезапись истории (git filter-repo) — деструктивно для форков/PR; выполняется только отдельным согласованным решением владельца (описано как опция).

## 4. Требования

- FR-1: **Аналитический этап (обязательный первый шаг)**: git-объём по классам (git-sizer, `git rev-list --objects` + сортировка по размеру), отчёт «кто растит историю» в docs_project/adr как основа решения.
- FR-2: **Git LFS для новых бинарей**: .gitattributes с отдельными поддерживаемыми Git-шаблонами `content/validation/**/*.jpg filter=lfs diff=lfs merge=lfs -text`, `content/validation/**/*.png filter=lfs diff=lfs merge=lfs -text`, `content/official-documents/originals/*.pdf filter=lfs diff=lfs merge=lfs -text`, `content/assets/**/*.jpg filter=lfs diff=lfs merge=lfs -text` и `content/assets/**/*.png filter=lfs diff=lfs merge=lfs -text` — существующие объекты остаются в истории (без переписывания), новые версии идут в LFS. Проверить совместимость: sha256-пины считаются по содержимому файла — LFS smudge отдаёт реальные байты, гейты работают; CI (actions/checkout с lfs: true) и Docker-сборка получают правку.
- FR-3: Валидационные evidence-скриншоты (105 МБ, только для аудитов): вынести из истории вперёд — новые evidence идут в LFS (FR-2) ИЛИ в отдельный evidence-репозиторий/релиз-артефакты; решение — ADR по итогам FR-1. Byte-exact сравнение сохраняется в любом варианте.
- FR-4: Мастера изображений из ТЗ-P3 (`content/assets-masters/`) — сразу LFS либо не коммитятся (воспроизводимы из PDF; команда генерации фиксируется в манифесте).
- FR-5: Опция полной перезаписи истории (filter-repo путём миграции всех бинарей в LFS, сокращение .git на порядок) описывается в ADR с процедурой и стоимостью (инвалидация всех клонов/PR); исполнение — только по явному решению владельца.
- NFR-1: `pnpm run validate:content` и все sha-гейты зелёные после каждого шага.

## 5. План

- [ ] 1. FR-1: отчёт git-sizer + раскладка по каталогам; ADR с рекомендацией
- [ ] 2. FR-2: LFS для новых бинарей; правка CI checkout и Dockerfile (COPY после lfs pull); прогон полного preflight
- [ ] 3. FR-3: решение и исполнение для validation/
- [ ] 4. FR-4: правило для мастеров (синхронно с ТЗ-P3 этап 1)
- [ ] 5. Документация: AGENTS.md/README — требования git lfs install для контрибьюторов

## 6. Критерии приёмки

- AC-1: ADR с данными FR-1 закоммичен.
- AC-2: `git check-attr filter` возвращает `lfs` для тестовых JPEG и PNG в `content/validation` и `content/assets`, но не назначает фильтр нетаргетному расширению; добавленные тестовые JPEG/PNG попадают в LFS (`git lfs ls-files`), гейты зелёные.
- AC-3: CI и docker-validation зелёные с LFS-checkout.
- AC-4: Через месяц после внедрения прирост .git на контент-PR ≤ 10 % прежнего (метрика в ADR).

## 7. Риски и меры

| Риск | Вероятн. | Влияние | Мера |
|---|---|---|---|
| LFS-квоты GitHub (1 ГБ storage/bandwidth бесплатно) | Средняя | Блок пуша/CI | Оценка объёмов в FR-1; при риске — evidence-репозиторий вместо LFS для validation/ |
| Забытый git lfs install у агента → указатели вместо файлов | Средняя | Красные sha-гейты | check-repo-baseline.mjs дополняется проверкой lfs-установки |
| Docker-сборка без lfs-объектов | Средняя | Битый образ | CI: checkout lfs:true до docker build; локально — предупреждение в Makefile |

## 8. Затрагиваемые файлы

`.gitattributes`, `.github/workflows/*.yml` (checkout lfs), `Dockerfile`/`Makefile` (предупреждения), `scripts/check-repo-baseline.mjs`, `docs_project/adr/*` (новый ADR), README/AGENTS.md.

## 9. Связанные ТЗ

[ТЗ-P3](./priority/03-image-quality.md), [ТЗ-18](./18-ci-preflight-optimization.md)
